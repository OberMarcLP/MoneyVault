services:
  frontend:
    image: ghcr.io/obermarclp/moneyvault/frontend:latest
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - BACKEND_URL=backend:8080
      - SITE_ADDRESS=${SITE_ADDRESS:-:80}
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    image: ghcr.io/obermarclp/moneyvault/backend:latest
    expose:
      - "8080"
    environment:
      - DATABASE_URL=postgres://moneyvault:${DB_PASSWORD}@db:5432/moneyvault?sslmode=disable
      - JWT_SECRET=${JWT_SECRET}
      - MIGRATIONS_PATH=file://migrations
      - ENVIRONMENT=production
      - PORT=8080
      - ALLOWED_ORIGIN=${ALLOWED_ORIGIN:-https://localhost}
      - TRUSTED_PROXIES=${TRUSTED_PROXIES:-}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    volumes:
      - pg_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=moneyvault
      - POSTGRES_USER=moneyvault
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U moneyvault"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pg_data:
  caddy_data:
  caddy_config:
