DB_PASSWORD=change-me-strong-password
JWT_SECRET=change-me-to-a-random-64-char-string
ALLOWED_ORIGIN=https://your-domain.com

# --- Reverse Proxy (optional) ---
# Set to your domain for automatic TLS, or leave as :80 behind a reverse proxy.
# SITE_ADDRESS=your-domain.com
# Disable Caddy's automatic HTTPS redirect (set to "off" behind a reverse proxy).
# AUTO_HTTPS=on
# Trusted proxy IPs/CIDRs for correct client IP detection (rate limiting, audit).
# Leave empty to trust all proxies (default).
# TRUSTED_PROXIES=172.18.0.0/16
# Host port mappings (change if your reverse proxy expects different ports).
# HTTP_PORT=80
# HTTPS_PORT=443
